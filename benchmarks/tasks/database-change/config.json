{
  "task_id": "database-change-001",
  "task_name": "Add Migration for New Column",
  "task_category": "database-change",
  "description": "Add a database migration to add a 'bio' column to the users table",
  "request": "Add a database migration that adds a 'bio' TEXT column to the users table. Make sure the migration is reversible.",
  "expected": {
    "max_files_changed": 3,
    "tests_must_pass": ["test_migrations.py"],
    "completion_required": true
  },
  "forbidden": {
    "no_new_files": false,
    "no_config_changes": true
  },
  "timeout_seconds": 300,
  "complexity": "medium",
  "risk": "high",
  "fast_path_applicable": false
}
